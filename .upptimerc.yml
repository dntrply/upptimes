# Change these first
owner: dntrply # Your GitHub organization or username, where this repository lives
repo: upptimes # The name of this repository

sites:
  - name: GEF - ADP
    url: https://api-adp.gurujifoundation.in/actuator/health
  - name: Test Broken Site
    url: https://thissitedoesnotexist.koj.co
  - name: IPv6 test
    url: forwardemail.net
    port: 80
    check: "tcp-ping"
    ipv6: true
  # Add your custom domain name, or remove the `cname` line if you don't have a domain
  # Uncomment the `baseUrl` line if you don't have a custom domain and add your repo name there
status-website:
  baseUrl: /upptimes # where "repo" is your repository name
  name: Your Status Website

# Upptime also supports notifications, assigning issues, and more
# See https://upptime.js.org/docs/configuration
